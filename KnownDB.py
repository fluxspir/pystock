#!/usr/bin/python3

"""
"""

from os import path

class CreateDB:
	def __init__(self, db, tblt, loct, tokt, prvdt):
		if not db or if not tblt or if not loct or if not entt, or \
			if not tokt or if not prvdt:
			print("Can't go further this way without db\n \
				Thank you for trying pyfinance !")
			quit()
	
	def makenewDB(self, db, tblt, loct, tokt, prvdt):
			

class KnownDB:
	def __init__(self, db="""os.path("~/.pyfinance.db")""", tblt="Metatable", \
				loct="worldstockexchange", entt="indices", tokt="stocks" \
				prvdt="yahoo", location="", entity="", token="", prvd=""):

		if not os.path.exists(db):
			CDb = CreateDb()

